
using System.Net.Cache;

var person = (name: "Rayhan", weight: 59, age: 25);
Console.WriteLine(person.name);

/*
 * Its an annonymos type because there is no class name here
 * good to use var datatype
 * Dynamic -> it will check the runtime but not check to the compiler
 * 
 */